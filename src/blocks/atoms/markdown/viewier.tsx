import React from "react";
import ReactMarkdown from "react-markdown";

const MarkDownviwer = ({ d }: any) => {
  return <div>{/* <ReactMarkdown>{d}</ReactMarkdown> */}</div>;
};

export default MarkDownviwer;
